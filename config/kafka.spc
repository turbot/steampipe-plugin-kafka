connection "kafka" {
  plugin = "kafka"

  # bootstrap_servers - List of host:port addresses that will be used to discover the full set of alive brokers. Required
  # bootstrap_servers = ["localhost:9092"]

}
